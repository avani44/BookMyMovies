# BookMyMovies
A console-based movie ticket booking system built using Core Java, JDBC, and MySQL. The application manages movies, theaters, shows, seat availability, and bookings with proper transaction handling to prevent double booking. Designed to simulate real-world backend booking logic with secure database operations.
